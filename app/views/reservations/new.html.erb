<div class="h-full w-full mt-16 rounded-2xl shadow-md border border-gray-200 flex items-center justify-center p-20 flex-col">
    <h1 class="text-3xl font-semibold mt-6 text-center w-full mb-3">Nueva reserva</h1>
    <section class='flex w-full flex-row justify-center items-center'>
        <div class='w-1/2 flex justify-center items-center p-2 '>
            <% @car.images.each do |car_image| %>
                <%= image_tag(car_image, class:'object-cover w-2/3 rounded') %>
            <% end %>
        </div>
        <div class='w-1/2 flex justify-center items-center'>
            <%= render 'reservations/form' %>
        </div>
    </section>
</div>





